modules:
  - type: rpm-ostree
    install:
      - nodejs20
      - nodejs20-npm
  - type: script
    scripts:
      - "system/install-prey.sh"
  - type: files
    files:
      - source: system/prey
        destination:  /
  - type: systemd
    system:
      enabled:
        - prey-agent.service
