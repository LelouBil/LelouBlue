modules:
  - type: rpm-ostree
    install:
      - ydotool
      - strace
      - wev
      - htop
      - mc
  - type: systemd
    system:
      enabled:
        - ydotool.service
  - type: files
    files:
      - source: system/system-apps/distrobox
        destination:  /
